# Chondro Breeder generated trait art

These `.b64` files are compact WebP payloads used by `scripts/materialize-assets.mjs` during `predev` and `prebuild`.

The build materializes them into `public/hatchery/snakes/traits/*.webp` so the breeder game can select artwork by subspecies, dominant trait, and expression tier.
