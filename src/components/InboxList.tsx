"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
type Row={conversation_id:string;conversations:{id:string;listing_id:string|null;updated_at:string;marketplace_listings:{title:string;image_urls:string[]}|null}|null};
export function InboxList(){const [rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/messages").then(r=>r.json()).then(d=>setRows(d.rows??[])).finally(()=>setLoading(false))},[]);if(loading)return <div className="panel rounded-3xl p-10 text-center text-sm text-white/30">Loading conversations…</div>;if(!rows.length)return <div className="panel rounded-3xl py-16 text-center"><div className="font-semibold text-white/55">No conversations yet.</div><p className="mt-2 text-xs text-white/28">Open a Marketplace listing and choose Message seller to start one.</p></div>;return <div className="space-y-3">{rows.map(r=>{const c=r.conversations,l=c?.marketplace_listings;return <Link href={`/messages/${r.conversation_id}`} key={r.conversation_id} className="panel flex items-center gap-4 rounded-2xl p-4 transition hover:bg-white/[.025]"><div className="grid-surface h-16 w-20 shrink-0 overflow-hidden rounded-xl">{l?.image_urls?.[0]&&<img src={l.image_urls[0]} alt="" className="h-full w-full object-cover"/>}</div><div className="min-w-0"><div className="text-[9px] font-black uppercase tracking-[.13em] text-emerald-300/50">Marketplace</div><div className="mt-1 truncate font-semibold">{l?.title||"Private conversation"}</div></div><span className="ml-auto text-white/20">→</span></Link>})}</div>}
