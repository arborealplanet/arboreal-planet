"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
type Row={conversation_id:string;listing_title:string|null;listing_image:string|null;other_display_name:string|null;other_username:string|null;other_avatar_url:string|null;last_message:string|null;unread_count:number};
export function InboxList(){const [rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/messages").then(r=>r.json()).then(d=>setRows(d.rows??[])).finally(()=>setLoading(false))},[]);if(loading)return <div className="panel rounded-3xl p-10 text-center text-sm text-white/30">Loading conversations…</div>;if(!rows.length)return <div className="panel rounded-3xl py-16 text-center"><div className="font-semibold text-white/55">No conversations yet.</div><p className="mt-2 text-xs text-white/28">Open a Marketplace listing and choose Message seller to start one.</p></div>;return <div className="space-y-3">{rows.map(r=><Link href={`/messages/${r.conversation_id}`} key={r.conversation_id} className="panel flex items-center gap-4 rounded-2xl p-4 transition hover:bg-white/[.025]"><div className="grid-surface h-16 w-20 shrink-0 overflow-hidden rounded-xl">{r.listing_image?<img src={r.listing_image} alt="" className="h-full w-full object-cover"/>:""}</div><div className="min-w-0 flex-1"><div className="text-[9px] font-black uppercase tracking-[.13em] text-emerald-300/50">{r.other_display_name||r.other_username||"Arboreal Planet user"} · Marketplace</div><div className="mt-1 truncate font-semibold">{r.listing_title||"Private conversation"}</div>{r.last_message&&<div className="mt-1 truncate text-xs text-white/30">{r.last_message}</div>}</div>{Number(r.unread_count)>0?<span className="grid h-7 min-w-7 place-items-center rounded-full bg-emerald-300 px-2 text-[10px] font-black text-[#06100c]">{r.unread_count}</span>:<span className="text-white/20">→</span>}</Link>)}</div>}
