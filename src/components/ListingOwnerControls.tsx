"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function ListingOwnerControls({id,status}:{id:string;status:string}){const router=useRouter(),[busy,setBusy]=useState(false);async function update(next:string){setBusy(true);const r=await fetch(`/api/marketplace/listings/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:next})});setBusy(false);if(r.ok)router.refresh()}async function remove(){if(!confirm("Remove this listing?"))return;setBusy(true);const r=await fetch(`/api/marketplace/listings/${id}`,{method:"DELETE"});if(r.ok)router.push("/marketplace");else setBusy(false)}return <div className="flex flex-wrap gap-2">{status==="ACTIVE"&&<button disabled={busy} onClick={()=>update("SOLD")} className="rounded-xl bg-emerald-300 px-4 py-2.5 text-xs font-black text-[#06100c]">Mark sold</button>}{status==="SOLD"&&<button disabled={busy} onClick={()=>update("ACTIVE")} className="rounded-xl border border-emerald-300/20 px-4 py-2.5 text-xs font-bold text-emerald-200">Relist</button>}<button disabled={busy} onClick={remove} className="rounded-xl border border-red-300/15 px-4 py-2.5 text-xs font-bold text-red-200/70">Remove</button></div>}
