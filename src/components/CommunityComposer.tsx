"use client";
import { useMemo,useState } from "react";
import { useRouter } from "next/navigation";
const postTypes=[["Post","POST"],["Question","QUESTION"],["Poll","POLL"],["Breeding update","BREEDING_UPDATE"]] as const;
const tags=["Green Tree Python","Boiga","Tree Monitors","Nepenthes","Breeding","Husbandry","Enclosures"];
export function CommunityComposer(){const router=useRouter(),[type,setType]=useState("POST"),[text,setText]=useState(""),[selectedTags,setSelectedTags]=useState<string[]>(["Green Tree Python"]),[busy,setBusy]=useState(false),[error,setError]=useState("");const ready=text.trim().length>0;const count=useMemo(()=>text.length,[text]);function toggleTag(tag:string){setSelectedTags(c=>c.includes(tag)?c.filter(i=>i!==tag):[...c,tag].slice(0,4))}async function publish(){if(!ready||busy)return;setBusy(true);setError("");const r=await fetch("/api/community/posts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type,body:text,tags:selectedTags})});if(r.status===401){location.href="/login?next=/community";return}if(!r.ok){setError("Could not publish that post.");setBusy(false);return}setText("");setBusy(false);router.refresh();window.dispatchEvent(new Event("community-posted"))}const label=postTypes.find(x=>x[1]===type)?.[0]??"Post";return <div className="panel overflow-hidden rounded-3xl"><div className="flex flex-wrap gap-1 border-b border-white/[.06] p-3">{postTypes.map(([name,value])=><button key={value} onClick={()=>setType(value)} className={`rounded-lg px-3 py-2 text-[11px] font-bold transition ${type===value?"bg-emerald-300 text-[#06100c]":"text-white/36 hover:bg-white/[.035] hover:text-white/60"}`}>{name}</button>)}</div><div className="p-5"><textarea value={text} onChange={e=>setText(e.target.value.slice(0,1200))} rows={4} placeholder={type==="QUESTION"?"Ask the keeper community something useful...":type==="BREEDING_UPDATE"?"Share a pairing, clutch, hatch or breeding observation...":"Share an update with the keeper community..."} className="w-full resize-none bg-transparent text-sm leading-6 text-white outline-none placeholder:text-white/22"/><div className="mt-3 flex flex-wrap gap-2">{tags.map(tag=><button key={tag} onClick={()=>toggleTag(tag)} className={`rounded-full border px-3 py-1.5 text-[10px] ${selectedTags.includes(tag)?"border-emerald-300/20 bg-emerald-300/[.07] text-emerald-200":"border-white/[.07] text-white/30"}`}>{tag}</button>)}</div>{error&&<div className="mt-3 text-xs text-red-200/70">{error}</div>}</div><div className="flex items-center justify-between border-t border-white/[.06] bg-black/10 px-5 py-4"><span className="text-[9px] text-white/20">{count}/1200 · {label}</span><button onClick={publish} disabled={!ready||busy} className={`rounded-lg px-4 py-2 text-[11px] font-bold ${ready&&!busy?"bg-emerald-300 text-[#06100c]":"bg-white/[.04] text-white/18"}`}>{busy?"Publishing…":"Publish"}</button></div></div>}
