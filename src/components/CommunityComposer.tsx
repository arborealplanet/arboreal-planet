"use client";
import { useMemo,useState } from "react";
import { useRouter } from "next/navigation";
const postTypes=[["Post","POST"],["Question","QUESTION"],["Breeding update","BREEDING_UPDATE"]] as const;
const tags=["Green Tree Python","Boiga","Tree Monitors","Nepenthes","Breeding","Husbandry","Enclosures"];
const MAX_IMAGES=10;
export function CommunityComposer(){const router=useRouter(),[type,setType]=useState("POST"),[text,setText]=useState(""),[selectedTags,setSelectedTags]=useState<string[]>(["Green Tree Python"]),[files,setFiles]=useState<File[]>([]),[video,setVideo]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");const ready=text.trim().length>0||files.length>0||video.trim().length>0;const count=useMemo(()=>text.length,[text]);function toggleTag(tag:string){setSelectedTags(c=>c.includes(tag)?c.filter(i=>i!==tag):[...c,tag].slice(0,4))}async function publish(){if(!ready||busy)return;setBusy(true);setError("");let media_urls:string[]=[];if(files.length){const form=new FormData();files.forEach(f=>form.append("images",f));const u=await fetch("/api/community/upload",{method:"POST",body:form});const d=await u.json().catch(()=>({}));if(!u.ok){setError(d.error||"Could not upload photos.");setBusy(false);return}media_urls=d.urls??[]}const r=await fetch("/api/community/posts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type,body:text,tags:selectedTags,media_urls,video_url:video})});if(r.status===401){router.push("/login?next=/community");return}const d=await r.json().catch(()=>({}));if(!r.ok){setError(d.error||"Could not publish that post.");setBusy(false);return}setText("");setFiles([]);setVideo("");setBusy(false);router.refresh();window.dispatchEvent(new Event("community-posted"))}const label=postTypes.find(x=>x[1]===type)?.[0]??"Post";return <div className="panel overflow-hidden rounded-3xl"><div className="flex flex-wrap gap-1 border-b border-white/[.06] p-3">{postTypes.map(([name,value])=><button key={value} onClick={()=>setType(value)} className={`rounded-lg px-3 py-2 text-[11px] font-bold transition ${type===value?"bg-emerald-300 text-[#06100c]":"text-white/36 hover:bg-white/[.035] hover:text-white/60"}`}>{name}</button>)}</div><div className="p-5"><textarea value={text} onChange={e=>setText(e.target.value.slice(0,1200))} rows={4} placeholder={type==="QUESTION"?"Ask the keeper community something useful...":type==="BREEDING_UPDATE"?"Share a pairing, clutch, hatch or breeding observation...":"Share an update with the keeper community..."} className="w-full resize-none bg-transparent text-sm leading-6 text-white outline-none placeholder:text-white/22"/><div className="mt-3 flex flex-wrap gap-2">{tags.map(tag=><button key={tag} onClick={()=>toggleTag(tag)} className={`rounded-full border px-3 py-1.5 text-[10px] ${selectedTags.includes(tag)?"border-emerald-300/20 bg-emerald-300/[.07] text-emerald-200":"border-white/[.07] text-white/30"}`}>{tag}</button>)}</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="cursor-pointer rounded-2xl border border-dashed border-white/10 p-4 text-xs text-white/40">Add photos · up to {MAX_IMAGES}<input type="file" accept="image/jpeg,image/png,image/webp" multiple className="hidden" onChange={e=>setFiles(Array.from(e.target.files??[]).slice(0,MAX_IMAGES))}/><span className="mt-1 block text-[10px] text-white/22">JPG, PNG or WebP · 10 MB each</span></label><label className="text-xs text-white/40">Video link<input value={video} onChange={e=>setVideo(e.target.value.slice(0,500))} placeholder="https://..." className="mt-2 w-full rounded-xl border border-white/[.07] bg-black/15 px-3 py-2.5 text-sm text-white outline-none"/></label></div>{files.length>0&&<div className="mt-3 flex flex-wrap gap-2">{files.map((f,i)=><div key={`${f.name}-${i}`} className="rounded-lg border border-white/[.07] px-3 py-2 text-[10px] text-white/35">{f.name}<button onClick={()=>setFiles(c=>c.filter((_,x)=>x!==i))} className="ml-2 text-red-200/70">×</button></div>)}</div>}{error&&<div className="mt-3 text-xs text-red-200/70">{error}</div>}</div><div className="flex items-center justify-between border-t border-white/[.06] bg-black/10 px-5 py-4"><span className="text-[9px] text-white/20">{count}/1200 · {label}{files.length?` · ${files.length} photo${files.length===1?"":"s"}`:""}</span><button onClick={publish} disabled={!ready||busy} className={`rounded-lg px-4 py-2 text-[11px] font-bold ${ready&&!busy?"bg-emerald-300 text-[#06100c]":"bg-white/[.04] text-white/18"}`}>{busy?"Publishing…":"Publish"}</button></div></div>}
