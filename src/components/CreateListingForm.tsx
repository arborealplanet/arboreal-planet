"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
const categories=["ANIMAL","PLANT","ENCLOSURE","SUPPLY","FEEDER"];
export function CreateListingForm(){const router=useRouter();const [category,setCategory]=useState("ANIMAL"),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());body.category=category;const r=await fetch("/api/marketplace/listings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(r.status===401){router.push("/login?next=/marketplace/new");return}const d=await r.json();if(!r.ok){setError(d.error||"Could not create listing");setBusy(false);return}router.push("/marketplace");router.refresh()}
return <form onSubmit={submit} className="panel rounded-3xl p-5 sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs text-white/45">Category<select value={category} onChange={e=>setCategory(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-[#08130e] p-3 text-white">{categories.map(c=><option key={c}>{c}</option>)}</select></label><label className="text-xs text-white/45">Price · USD<input name="price" required type="number" min="0" step="0.01" className="mt-2 w-full rounded-xl border border-white/10 bg-black/15 p-3 text-white"/></label><label className="sm:col-span-2 text-xs text-white/45">Listing title<input name="title" required maxLength={140} className="mt-2 w-full rounded-xl border border-white/10 bg-black/15 p-3 text-white"/></label><label className="sm:col-span-2 text-xs text-white/45">Description<textarea name="description" rows={5} className="mt-2 w-full rounded-xl border border-white/10 bg-black/15 p-3 text-white"/></label>{category==="ANIMAL"&&<><label className="text-xs text-white/45">Origin<select name="public_origin" className="mt-2 w-full rounded-xl border border-white/10 bg-[#08130e] p-3 text-white"><option value="CAPTIVE_BRED">Captive Bred</option><option value="IMPORT">Import</option></select></label><label className="text-xs text-white/45">Sex<select name="sex" className="mt-2 w-full rounded-xl border border-white/10 bg-[#08130e] p-3 text-white"><option value="">Unspecified</option><option>Male</option><option>Female</option></select></label><label className="text-xs text-white/45">Morph / locality<input name="morph" className="mt-2 w-full rounded-xl border border-white/10 bg-black/15 p-3 text-white"/></label><label className="text-xs text-white/45">Age / birth year<input name="age_or_year" className="mt-2 w-full rounded-xl border border-white/10 bg-black/15 p-3 text-white"/></label><label className="sm:col-span-2 text-xs text-white/45">Lineage / parents<input name="lineage" className="mt-2 w-full rounded-xl border border-white/10 bg-black/15 p-3 text-white"/></label></>}<label className="sm:col-span-2 text-xs text-white/45">Seller location<input name="seller_location" placeholder="City, State" className="mt-2 w-full rounded-xl border border-white/10 bg-black/15 p-3 text-white"/></label></div>{error&&<div className="mt-4 text-sm text-red-300">{error}</div>}<button disabled={busy} className="mt-6 rounded-xl bg-emerald-300 px-5 py-3 text-sm font-black text-[#06100c] disabled:opacity-50">{busy?"Publishing…":"Publish listing"}</button><p className="mt-3 text-xs text-white/28">Photo uploads are the next marketplace pass. This creates a real listing tied to your signed-in account.</p></form>}
