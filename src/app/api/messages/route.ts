import { NextResponse } from "next/server";
import { getServerIdentity,SUPABASE_AUTH_KEY,SUPABASE_AUTH_URL } from "@/lib/supabase-auth";
export async function GET(){const identity=await getServerIdentity();if(!identity)return NextResponse.json({error:"Sign in required"},{status:401});const response=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/rpc/conversation_summary`,{method:"POST",headers:{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${identity.token}`,"Content-Type":"application/json"},body:"{}",cache:"no-store"});if(!response.ok)return NextResponse.json({error:"Could not load conversations"},{status:502});return NextResponse.json({rows:await response.json()});}
