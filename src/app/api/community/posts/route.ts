import { NextRequest,NextResponse } from "next/server";
import { getServerIdentity,SUPABASE_AUTH_KEY,SUPABASE_AUTH_URL } from "@/lib/supabase-auth";
const base={apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${SUPABASE_AUTH_KEY}`};
const MAX_IMAGES=10;
function cleanVideo(raw:unknown){const value=String(raw??"").trim();if(!value)return null;if(value.length>500)return false;try{const u=new URL(value);if(u.protocol!=="https:"||u.username||u.password)return false;return u.toString()}catch{return false}}
export async function GET(request:NextRequest){const identity=await getServerIdentity();const mode=request.nextUrl.searchParams.get("mode")==="following"?"following":"explore";if(mode==="following"&&!identity)return NextResponse.json({rows:[],viewerId:null,authRequired:true});const rpc=mode==="following"?"community_following_feed":"community_feed";const headers=mode==="following"&&identity?{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${identity.token}`,"Content-Type":"application/json"}:{...base,"Content-Type":"application/json"};const r=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/rpc/${rpc}`,{method:"POST",headers,body:"{}",cache:"no-store"});return r.ok?NextResponse.json({rows:await r.json(),viewerId:identity?.user.id??null,authRequired:false}):NextResponse.json({error:"Community unavailable"},{status:502});}
export async function POST(request:NextRequest){const identity=await getServerIdentity();if(!identity)return NextResponse.json({error:"Sign in required"},{status:401});const b=await request.json();const body=String(b.body??"").trim();const types=new Set(["POST","QUESTION","POLL","BREEDING_UPDATE"]);const type=String(b.type??"POST");const tags=Array.isArray(b.tags)?b.tags.map(String).map((x:string)=>x.trim()).filter(Boolean).slice(0,4):[];const media=Array.isArray(b.media_urls)?b.media_urls.filter((x:unknown):x is string=>typeof x==="string").slice(0,MAX_IMAGES):[];const prefix=`${SUPABASE_AUTH_URL}/storage/v1/object/public/community/${identity.user.id}/`;if(media.some((url:string)=>!url.startsWith(prefix)))return NextResponse.json({error:"Invalid community image"},{status:400});const video=cleanVideo(b.video_url);if(video===false)return NextResponse.json({error:"Video link must be a valid HTTPS URL"},{status:400});if(!types.has(type)||(!body&&media.length===0&&!video)||body.length>1200)return NextResponse.json({error:"Invalid post"},{status:400});const r=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/community_posts`,{method:"POST",headers:{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${identity.token}`,"Content-Type":"application/json",Prefer:"return=representation"},body:JSON.stringify({author_id:identity.user.id,type,body,tags,media_urls:media,video_url:video}),cache:"no-store"});return r.ok?NextResponse.json({post:(await r.json())[0]},{status:201}):NextResponse.json({error:"Could not publish post"},{status:400});}
