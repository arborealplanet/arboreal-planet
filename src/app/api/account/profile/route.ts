import { NextResponse } from "next/server";
import { fetchOwnProfile, getServerIdentity, SUPABASE_AUTH_KEY, SUPABASE_AUTH_URL } from "@/lib/supabase-auth";

export async function GET(){const identity=await getServerIdentity();if(!identity)return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json({user:identity.user,profile:await fetchOwnProfile(identity.token,identity.user.id)});}

export async function PATCH(request:Request){const identity=await getServerIdentity();if(!identity)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await request.json().catch(()=>null) as Record<string,unknown>|null;if(!body)return NextResponse.json({error:"Invalid request"},{status:400});const allowed={username:body.username||null,display_name:body.display_name||null,bio:body.bio||null,location:body.location||null,avatar_url:body.avatar_url||null,banner_url:body.banner_url||null,accent_color:body.accent_color||"emerald",profile_visibility:body.profile_visibility==="private"?"private":"public",seller_enabled:Boolean(body.seller_enabled),updated_at:new Date().toISOString()};const response=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/profiles?id=eq.${encodeURIComponent(identity.user.id)}`,{method:"PATCH",headers:{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${identity.token}`,"Content-Type":"application/json",Prefer:"return=representation"},body:JSON.stringify(allowed),cache:"no-store"});const data=await response.json().catch(()=>null);if(!response.ok)return NextResponse.json({error:"Unable to update profile",detail:data},{status:response.status});return NextResponse.json({ok:true,profile:Array.isArray(data)?data[0]:data});}
