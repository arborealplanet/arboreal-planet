import { NextRequest,NextResponse } from "next/server";
import { getServerIdentity,SUPABASE_AUTH_KEY,SUPABASE_AUTH_URL } from "@/lib/supabase-auth";
const allowed=new Set(["ANIMAL","PLANT","ENCLOSURE","SUPPLY","FEEDER"]);
export async function GET(){const response=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/marketplace_listings?status=eq.ACTIVE&select=id,owner_id,category,title,description,species_id,locality_id,public_origin,price,currency,image_urls,seller_location,morph,sex,age_or_year,lineage,created_at&order=created_at.desc`,{headers:{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${SUPABASE_AUTH_KEY}`},cache:"no-store"});if(!response.ok)return NextResponse.json({error:"Listings unavailable"},{status:502});return NextResponse.json({rows:await response.json()});}
export async function POST(request:NextRequest){const identity=await getServerIdentity();if(!identity)return NextResponse.json({error:"Sign in required"},{status:401});const body=await request.json();if(!allowed.has(body.category))return NextResponse.json({error:"Invalid category"},{status:400});const price=Number(body.price);if(!body.title?.trim()||!Number.isFinite(price)||price<0)return NextResponse.json({error:"Title and valid price required"},{status:400});const images=Array.isArray(body.image_urls)?body.image_urls.filter((x:unknown)=>typeof x==="string").slice(0,10):[];const payload={owner_id:identity.user.id,category:body.category,title:body.title.trim(),description:body.description?.trim()||null,public_origin:body.category==="ANIMAL"?(body.public_origin||null):null,price,currency:"USD",seller_location:body.seller_location?.trim()||null,morph:body.morph?.trim()||null,sex:body.sex||null,age_or_year:body.age_or_year?.trim()||null,lineage:body.lineage?.trim()||null,status:"ACTIVE",image_urls:images};const response=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/marketplace_listings`,{method:"POST",headers:{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${identity.token}`,"Content-Type":"application/json",Prefer:"return=representation"},body:JSON.stringify(payload),cache:"no-store"});if(!response.ok)return NextResponse.json({error:await response.text()},{status:400});const rows=await response.json();return NextResponse.json({listing:rows[0]},{status:201});}
