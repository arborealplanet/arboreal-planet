import { NextRequest,NextResponse } from "next/server";
import { getServerIdentity,SUPABASE_AUTH_KEY,SUPABASE_AUTH_URL } from "@/lib/supabase-auth";
const statuses=new Set(["DRAFT","ACTIVE","SOLD","REMOVED"]),origins=new Set(["CAPTIVE_BRED","IMPORT"]),editable=new Set(["title","description","price","seller_location","public_origin","morph","sex","age_or_year","lineage","status","species_id","locality_id","image_urls"]),uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const publicHeaders={apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${SUPABASE_AUTH_KEY}`};
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const identity=await getServerIdentity();if(!identity)return NextResponse.json({error:"Sign in required"},{status:401});const {id}=await params;const body=await request.json();const entries=Object.entries(body).filter(([key])=>editable.has(key));if(!entries.length)return NextResponse.json({error:"No valid changes supplied"},{status:400});const payload:Record<string,unknown>=Object.fromEntries(entries);if("title" in payload){const title=String(payload.title??"").trim();if(!title||title.length>160)return NextResponse.json({error:"Invalid title"},{status:400});payload.title=title}if("price" in payload){const price=Number(payload.price);if(!Number.isFinite(price)||price<0)return NextResponse.json({error:"Invalid price"},{status:400});payload.price=price}if("status" in payload){const status=String(payload.status??"");if(!statuses.has(status))return NextResponse.json({error:"Invalid listing status"},{status:400});payload.status=status}if("public_origin" in payload){const origin=payload.public_origin==null||payload.public_origin===""?null:String(payload.public_origin);if(origin!==null&&!origins.has(origin))return NextResponse.json({error:"Invalid animal origin"},{status:400});payload.public_origin=origin}if("image_urls" in payload){if(!Array.isArray(payload.image_urls)||payload.image_urls.length>10)return NextResponse.json({error:"A listing can have up to 10 photos"},{status:400});const prefix=`${SUPABASE_AUTH_URL}/storage/v1/object/public/marketplace/${identity.user.id}/`;const images=payload.image_urls.filter((url:unknown):url is string=>typeof url==="string"&&url.startsWith(prefix));if(images.length!==payload.image_urls.length)return NextResponse.json({error:"Invalid marketplace photo"},{status:400});payload.image_urls=[...new Set(images)]}if("species_id" in payload||"locality_id" in payload){const speciesId=String(payload.species_id??"")||null,localityId=String(payload.locality_id??"")||null;if(speciesId&& !uuid.test(speciesId))return NextResponse.json({error:"Invalid species"},{status:400});if(localityId&&(!speciesId||!uuid.test(localityId)))return NextResponse.json({error:"Invalid locality"},{status:400});if(speciesId){const s=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/species?id=eq.${speciesId}&published=eq.true&select=id`,{headers:publicHeaders,cache:"no-store"});if(!(s.ok&&(await s.json()).length))return NextResponse.json({error:"Species is not available"},{status:400})}if(localityId){const l=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/localities?id=eq.${localityId}&species_id=eq.${speciesId}&active=eq.true&select=id`,{headers:publicHeaders,cache:"no-store"});if(!(l.ok&&(await l.json()).length))return NextResponse.json({error:"Locality does not match selected species"},{status:400})}payload.species_id=speciesId;payload.locality_id=localityId}if("description" in payload)payload.description=String(payload.description??"").trim().slice(0,5000)||null;if("seller_location" in payload)payload.seller_location=String(payload.seller_location??"").trim().slice(0,160)||null;if("morph" in payload)payload.morph=String(payload.morph??"").trim().slice(0,160)||null;if("sex" in payload)payload.sex=String(payload.sex??"").trim().slice(0,40)||null;if("age_or_year" in payload)payload.age_or_year=String(payload.age_or_year??"").trim().slice(0,80)||null;if("lineage" in payload)payload.lineage=String(payload.lineage??"").trim().slice(0,2000)||null;const response=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/marketplace_listings?id=eq.${encodeURIComponent(id)}&owner_id=eq.${identity.user.id}`,{method:"PATCH",headers:{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${identity.token}`,"Content-Type":"application/json",Prefer:"return=representation"},body:JSON.stringify(payload),cache:"no-store"});if(!response.ok)return NextResponse.json({error:await response.text()},{status:400});const rows=await response.json();return rows.length?NextResponse.json({listing:rows[0]}):NextResponse.json({error:"Listing not found"},{status:404})}
export async function DELETE(_:NextRequest,{params}:{params:Promise<{id:string}>}){const identity=await getServerIdentity();if(!identity)return NextResponse.json({error:"Sign in required"},{status:401});const {id}=await params;const response=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/marketplace_listings?id=eq.${encodeURIComponent(id)}&owner_id=eq.${identity.user.id}`,{method:"DELETE",headers:{apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${identity.token}`,Prefer:"return=representation"},cache:"no-store"});if(!response.ok)return NextResponse.json({error:await response.text()},{status:400});const rows=await response.json();return rows.length?NextResponse.json({ok:true}):NextResponse.json({error:"Listing not found"},{status:404})}
