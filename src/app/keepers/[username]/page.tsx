import Link from "next/link";
import { notFound } from "next/navigation";
import { SUPABASE_AUTH_KEY,SUPABASE_AUTH_URL } from "@/lib/supabase-auth";

export const dynamic="force-dynamic";
type Profile={id:string;username:string;display_name:string|null;bio:string|null;location:string|null;avatar_url:string|null;banner_url:string|null;accent_color:string|null;seller_enabled:boolean;role:string|null};
type Listing={id:string;title:string;category:string;price:number|null;currency:string;image_urls:string[]|null;public_origin:string|null;morph:string|null;species:{common_name:string}|null;locality:{name:string}|null};

export default async function KeeperProfilePage({params}:{params:Promise<{username:string}>}){
 const {username}=await params;const clean=decodeURIComponent(username).trim();if(!clean)notFound();const headers={apikey:SUPABASE_AUTH_KEY,Authorization:`Bearer ${SUPABASE_AUTH_KEY}`};
 const p=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/profiles?username=eq.${encodeURIComponent(clean)}&profile_visibility=eq.public&select=id,username,display_name,bio,location,avatar_url,banner_url,accent_color,seller_enabled,role`,{headers,cache:"no-store"});if(!p.ok)notFound();const profiles:Profile[]=await p.json();const profile=profiles[0];if(!profile)notFound();
 let listings:Listing[]=[];if(profile.seller_enabled){const select="id,title,category,price,currency,image_urls,public_origin,morph,species:species!marketplace_listings_species_id_fkey(common_name),locality:localities!marketplace_listings_locality_id_fkey(name)";const l=await fetch(`${SUPABASE_AUTH_URL}/rest/v1/marketplace_listings?owner_id=eq.${profile.id}&status=eq.ACTIVE&select=${encodeURIComponent(select)}&order=created_at.desc`,{headers,cache:"no-store"});if(l.ok)listings=await l.json()}
 return <main className="mx-auto max-w-6xl px-5 py-10 sm:px-6"><Link href="/marketplace" className="text-xs font-bold text-emerald-200/70">← Marketplace</Link><section className="panel mt-5 overflow-hidden rounded-[32px]"><div className="h-44 bg-white/[.025] bg-cover bg-center sm:h-56" style={profile.banner_url?{backgroundImage:`url(${profile.banner_url})`}:undefined}/><div className="relative p-6 pt-16 sm:p-8 sm:pt-20"><div className="absolute -top-12 left-6 h-24 w-24 overflow-hidden rounded-3xl border-4 border-[#07110d] bg-white/[.05] sm:left-8 sm:h-28 sm:w-28">{profile.avatar_url?<img src={profile.avatar_url} alt={`${profile.display_name||profile.username} avatar`} className="h-full w-full object-cover"/>:<div className="grid h-full w-full place-items-center text-2xl text-white/15">AP</div>}</div><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-3xl font-semibold">{profile.display_name||profile.username}</div><div className="mt-1 text-sm text-white/35">@{profile.username}</div></div><div className="flex gap-2">{profile.seller_enabled&&<span className="rounded-full border border-emerald-300/15 bg-emerald-300/[.05] px-3 py-1.5 text-[10px] font-black uppercase tracking-[.12em] text-emerald-200/70">Seller</span>}<span className="rounded-full border border-white/[.07] px-3 py-1.5 text-[10px] uppercase text-white/35">{profile.role||"user"}</span></div></div>{profile.bio&&<p className="mt-6 max-w-3xl whitespace-pre-wrap text-sm leading-7 text-white/55">{profile.bio}</p>}{profile.location&&<div className="mt-4 text-xs text-white/32">{profile.location}</div>}</div></section><section className="mt-8"><div className="mb-4 flex items-end justify-between"><div><div className="section-kicker">Marketplace inventory</div><h2 className="mt-2 text-2xl font-semibold">Active listings</h2></div><div className="text-xs text-white/30">{listings.length} active</div></div>{listings.length?<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{listings.map(l=><Link key={l.id} href={`/marketplace/${l.id}`} className="panel overflow-hidden rounded-3xl"><div className="grid-surface grid h-44 place-items-center overflow-hidden border-b border-white/[.06]">{l.image_urls?.[0]?<img src={l.image_urls[0]} alt={l.title} className="h-full w-full object-cover"/>:<span className="text-white/15">◇</span>}</div><div className="p-5"><div className="text-[9px] font-black uppercase tracking-[.14em] text-emerald-300/55">{l.category}{l.public_origin?` · ${l.public_origin==="CAPTIVE_BRED"?"Captive Bred":"Import"}`:""}</div><h3 className="mt-2 font-semibold">{l.title}</h3>{l.species&&<div className="mt-1 text-xs text-white/40">{l.species.common_name}{l.locality?.name?` · ${l.locality.name}`:""}</div>}<div className="mt-3 text-xl font-semibold">{l.price==null?"Contact seller":`$${Number(l.price).toLocaleString()}`}</div>{l.morph&&<div className="mt-2 text-xs text-white/30">{l.morph}</div>}</div></Link>)}</div>:<div className="panel rounded-3xl py-12 text-center text-sm text-white/35">No active marketplace listings right now.</div>}</section></main>
}
